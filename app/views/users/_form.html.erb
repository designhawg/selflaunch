<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :city_id %><br />
    <%= f.text_field :city_id %>
  </div>
  <div class="field">
    <%= f.label :country_id %><br />
    <%= f.select :country_id, User::COUNTRY%>
  </div>
  <div class="field">
    <%= f.label :state_id %><br />
    <%= f.select :state_id, User::STATE%>
  </div>
  <div class="actions">
    <%= f.submit "Create"%>
  </div>
<% end %>
